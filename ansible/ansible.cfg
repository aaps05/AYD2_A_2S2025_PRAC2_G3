[defaults]
inventory          = inventory/hosts.yml
remote_user        = ubuntu
private_key_file   = ~/.ssh/ayd-keypair.pem
host_key_checking  = False
stdout_callback    = yaml
timeout            = 60

[ssh_connection]
pipelining = True
